import React from 'react';
import styles from './hero.module.css';

function Hero() {
  return (
    <section id="hero" className={styles.hero}>
      <h1>Hero</h1>
    </section>
  );
}

export default Hero;
